[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v 
    --tb=short
    --strict-markers
    --disable-warnings
markers =
    auth: Authentication related tests
    smoke: Smoke tests
    slow: Slow running tests